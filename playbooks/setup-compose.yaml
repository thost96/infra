---
- name: Deploy Docker Pre-Requisites
  hosts: compose
  become: yes
  tasks:
    - name: Ensure docker is installed
      ansible.builtin.package:
        name: docker-ce
        state: present

    - name: Ensure docker compose is installed
      ansible.builtin.package:
        name: docker-compose-plugin
        state: present
    
    - name: Ensure python-docker for ansible use is installed
      ansible.builtin.package:
        name: python-docker
        state: present

    - name: Create Folder /docker if it does not exist
      ansible.builtin.file:
        state: directory
        path: /docker
        mode: "0755"
