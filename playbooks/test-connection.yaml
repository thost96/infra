---
- name: Test SSH Service Connection
  hosts: all
  become: false
  gather_facts: false
  tasks:
    - name: Check if ssh is accessible
      ansible.builtin.wait_for:
        host: "{{ inventory_hostname }}"
        port: 22
        state: started
        delay: 10
        timeout: 30
