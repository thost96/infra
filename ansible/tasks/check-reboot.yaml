---
- name: Check for pending reboot on Debian-based systems
  stat:
    path: /var/run/reboot-required
  register: reboot_required

- name: Display reboot status
  debug:
    msg: "Reboot required: {{ reboot_required.stat.exists }}"
