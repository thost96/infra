---
services:
  app:
    image: ghcr.io/itskovacs/wingfit:5
    ports:
      - 8080:8000
    volumes:
      - ./storage:/app/storage #Do not change /app/storage, only the first part (./storage) if needed
    command: ["fastapi", "run", "/app/wingfit/main.py", "--host", "0.0.0.0"]
# https://github.com/itskovacs/wingfit/blob/main/docker-compose.yml
